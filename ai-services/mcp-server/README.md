# MCP Server

Release 1 requirement (see project spec section 4.2). Not implemented yet
— Release 0 work only requires the AI-mode capability in `../ai-mode/`.

When Release 1 starts, this is where the shared Model Context Protocol
server implementation should live.
